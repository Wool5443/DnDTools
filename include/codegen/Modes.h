// #define DEF_MODE(modmode, modname, moddefaultstate, modfunction, ...)

DEF_MODE(
    DICE_MODE,
    "dice",
    DEFAULT_DICE_MODE,
    DiceMode
)

DEF_MODE(
    STATS_MODE,
    "stats",
    DEFAULT_STATS_MODE,
    StatsMode
)
